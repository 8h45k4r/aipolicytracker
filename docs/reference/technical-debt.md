# Technical debt register

Every accepted gap has an owner. Remove entries only when the fix is merged.

| # | Debt | Gate | Owner | Accepted | Exit criterion |
|---|------|------|-------|----------|----------------|
| 1 | ~~UI primitives missing in the React admin.~~ **Resolved 2026-09-11**: the React admin is disabled (debt #17); the Blade admin uses the shared design-system components. | UI/UX | @8h45k4r | 2026-09-11 | Done. |
| 2 | ~~Fictional sample policies in production.~~ **Resolved 2026-09-11**: production never ran the sample seeder (startup log: `Removed 0 sample policies.`); the source-backed legacy dataset loaded on deploy (`Policies: 68 created, 0 updated`) and the public API listed 68 entries across 37 jurisdictions. Row kept for audit trail. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 3 | ~~Legacy `ai_policy_trackers` rows have a single `whitepaper_document_link` and no verification date, verifier, or confidence. The policy-intelligence module now provides `source_documents` and verification fields for structured records; the legacy table is unchanged.~~ **Resolved 2026-09-11**: legacy `ai_policy_trackers` table and pages removed in #35; structured records carry full source-quality fields. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 4 | Row-level security is not available on the current stack (Laravel/Eloquent over PostgreSQL without RLS policies); scoping relies on `User::scopeNonAdmin`, `BookMark` user filters, and the `isAdmin` middleware. | Engineering | @8h45k4r | 2026-09-11 | Either Postgres RLS policies added for user-scoped tables or a documented decision that Eloquent scopes are sufficient. |
| 5 | `npm run typecheck` is a no-op because the front end is plain JavaScript. | Engineering | @8h45k4r | 2026-09-11 | TypeScript adopted or JSDoc `checkJs` enabled with a passing baseline. |
| 6 | Laravel Pint style check is advisory in CI (`continue-on-error`). | Engineering | @8h45k4r | 2026-09-11 | Run `composer lint:fix` once, commit, and make the step blocking. |
| 7 | Null values render as empty strings or `0` in several tables (`Backend/Users/Index.jsx`, dashboard counters). | UI/UX | @8h45k4r | 2026-09-11 | Shared `formatValue` helper renders `—` for null and is used everywhere. |
| 8 | ~~Legacy single-policy page (`/aipolicytracker/single-view/{id}`) requires a verified login. Structured policy pages (`/policies/{slug}`) are public; the legacy page is noindex and reachable only from `/map`.~~ **Resolved 2026-09-11**: legacy single-view page removed; `/aipolicytracker/single-view/*` redirects to `/policies` (#45). | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 9 | ~~CKEditor HTML is stored and rendered raw; DOMPurify is a dependency but not applied on every render path.~~ **Resolved 2026-09-11**: CKEditor and DOMPurify removed with the legacy CMS (#48); all public output is Blade-escaped. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 10 | ~~Mail transport is `log` in production; verification e-mails are not delivered.~~ **Resolved 2026-09-11**: Resend transport configured through Admin → Settings (#33/#43); verification, confirmation, digest and alert mail deliver. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 26 | The verification policy (2026-09-17) launched with `critical_budget` set to the backlog that existed on the day, so the gate prevents regression but does not yet force the backlog down. | Compliance | @8h45k4r | 2026-09-17 | The budget is lowered after every batch of verifications until it reaches zero; `/verification` shows the current standing. |
| 27 | Coverage is measured as completeness of the records that exist here, not as coverage of the world: `/coverage` can say how many published instruments lack a source date, and cannot say how many instruments exist that have never been recorded. Measuring the second needs a sourced denominator (a published list of instruments that ought to exist), which the project does not have. The page states this in those words rather than letting a percentage imply the stronger claim. | Compliance | @8h45k4r | 2026-09-17 | A denominator adopted from a citable published register per jurisdiction, with its own source and retrieval date, and `/coverage` reporting recorded-against-expected beside the completeness figures. |
| 11 | All structured policy records in `data/` are `review_status: pending_review` with no `last_verified_at`; official sources could not be fetched from the build environment. The UI labels every such record. | Compliance | @8h45k4r | 2026-09-11 | Each record verified by a named reviewer against its `official_source_url` (priority list in `IMPLEMENTATION_REPORT.md` §7); `verified` count on the homepage matches published count. |
| 12 | ~~Legacy `ai_policy_trackers` rows and structured `policy_instruments` are not cross-linked; `/map` and `/policies` can show the same instrument twice.~~ **Resolved 2026-09-11**: legacy rows dropped in #35; only structured `policy_instruments` remain. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 13 | `composer.lock` pins `nette/schema` 1.3.0, which rejects PHP 8.4; local setups on 8.4 need `--ignore-platform-req=php`. | Engineering | @8h45k4r | 2026-09-11 | Lock file refreshed with a PHP 8.4-compatible dependency set and CI matrix extended. |
| 14 | ~~Public CSS uses raw Tailwind palette classes.~~ **Resolved 2026-09-11**: brand and state tokens defined in `tailwind.config.js`; public templates, components and status enums use only `brand-*`/`state-*` tokens. Row kept for audit trail. | UI/UX | @8h45k4r | 2026-09-11 | Done. |
| 15 | ~~CI runs the PHP suite on in-memory SQLite only, so PostgreSQL-specific failures reach production undetected.~~ **Resolved 2026-09-18**: a `PHP tests (PostgreSQL)` job runs migrations, `policy:import` and the full suite against `postgres:16` on every pull request (#91). It found a second divergence on its first run: a constraint violation aborts the whole transaction in PostgreSQL (SQLSTATE 25P02) but not in SQLite, so the duplicate-webhook path in `WebhookProcessor` answered a provider's ordinary retry with a 500 instead of an acknowledgement. The insert is now wrapped in a savepoint and a test asserts the retry is acknowledged while a transaction is open. The original realisation stands as the audit trail: `/reviewers` returned 500 in production because `ReviewerRoster` queried `obligations.reviewed_by`, a column that does not exist; SQLite reads an unmatched double-quoted identifier as a string literal, so the query silently matched every row and the suite stayed green. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 17 | ~~Legacy map site gated but retained.~~ **Resolved 2026-09-11**: legacy controllers, models, React pages, seeders, sample dataset and tables removed; old URLs 301 to the new pages. | Engineering | @8h45k4r | 2026-09-11 | Done. |
| 18 | Global coverage expansion (2026-09-11/12) added 173 jurisdictions and 108 instruments as `pending_review` (95 of the jurisdictions record no AI-specific instrument and cite a government portal plus a labelled secondary source); 49 instruments are `confidence_level: low`; the 24 estimated adoption dates were removed on 2026-09-12 (dates are empty until sourced). Records are shown with their review status and confidence, never as verified. The admin review queue now records verifications durably and exports them to data/. | Documentation | @8h45k4r | 2026-09-11 | Reviewer opens each `official_source_url`, replaces placeholder dates with sourced dates or sets `confidence_level: unavailable`, and promotes records to `verified` with `last_verified_at` and `reviewed_by`. Track progress in the admin review queue ("never verified or stale"). |
| 19 | Free tools MVP (2026-09-11): seeded formats are XLSX/CSV/Markdown only (admins can upload PDF/DOCX; none generated yet), sign-in is email only (no Google/GitHub), no welcome email with links, email verification is requested but not enforced before download. | Engineering | @8h45k4r | 2026-09-11 | Add Socialite providers and a welcome mailable; generate PDF/DOCX in CI; move resources to a table with admin CRUD once the library grows beyond a handful of items. |
| 20 | Live incident sync (2026-09-12) depends on the AI Incident Database GraphQL endpoint, which only answers requests carrying the site's own `Origin` header; we send it with a User-Agent that names this project and pull at most every six hours. If AIID restricts the endpoint further, the weekly export path keeps the tables populated but the "latest" list lags by up to a week. | Compliance | @8h45k4r | 2026-09-12 | Written confirmation from the Responsible AI Collaborative that programmatic access is acceptable, or a documented public API/feed adopted in its place. |
| 21 | Billing (2026-09-12) ships with checkout off and no provider keys; Dodo business verification not yet completed. Provisioning (endpoint, products, keys) is one click in Admin → Billing once an API key is stored, and checkout is a switch in Admin → Settings, so test-mode selling needs no redeploy; live mode still needs verification. | Compliance | @8h45k4r | 2026-09-12 | Dodo verification approved, live provisioning run, Admin → Billing product check green, one test-mode purchase and one decline recorded in `billing_events`, then Checkout `on` in live_mode. |
| 22 | ~~Pro promised capabilities with no code behind them.~~ **Resolved 2026-09-12**: `history.full` and `api.requests_per_day` removed from the plans and from every customer-facing list; Pro now sells only what the alerts module delivers (`saved.server`, `alerts.daily`). | Engineering | @8h45k4r | 2026-09-12 | Done. |
| 25 | The change-impact engine (2026-09-17) screens against records that are almost entirely `pending_review`: 398 canonical records carry that status and none are `verified`. Alerts therefore say "may affect" and link the official source, and the tool repeats that screening is not a legal determination. The paid promise is timeliness, not a verdict. | Compliance | @8h45k4r | 2026-09-17 | Depth verification of the five jurisdictions that carry most paid usage (EU, US, UK, India, Singapore), recorded through the admin review queue with reviewer and date, before any wording upgrades relevance to a determination. |
| 24 | Billing sells one plan family (Pro monthly/annual): no Team plan with seats, no full change-history product and no metered API keys. Plan changes, invoices and cancellation happen only in the provider portal. | Engineering | @8h45k4r | 2026-09-12 | Each new capability ships with its entitlement read by `Entitlements` and only then appears on the pricing page; Team plan added once a second buyer persona is validated. |
| 23 | Webhook processing runs inline in the request; a provider retry storm or a slow database would be absorbed only by the 120/min throttle. Provider API calls (checkout, portal, product check) are synchronous with the SDK's default timeout. | Engineering | @8h45k4r | 2026-09-12 | Move `WebhookProcessor::handle` behind a queued job with the event row created in the request, once `queue:work` runs in production. |
| 28 | `user_infos.ip_address` stores the sign-up address in full, while every other address in the system is hashed (`resource_downloads.ip_hash`, `admin_audit_logs.ip_hash`). The published privacy policy (2026-09-18) states this in plain words rather than glossing it, but the inconsistency is real: an address kept in full is personal data that a hash would not be. | Compliance | @8h45k4r | 2026-09-18 | `user_infos.ip_address` replaced by `ip_hash` using the same keyed hash as the other two tables, existing rows migrated by hashing in place, and the privacy policy row for it removed. |
| 29 | The privacy policy and terms (2026-09-18) ship without a governing-law clause and, unless `LEGAL_PRIVACY_EMAIL` or `CONTACT_EMAILS` is set, without a named address for data requests. Neither can be derived from the codebase, and the pages omit the clause rather than stating an invented jurisdiction or mailbox. Both pages are otherwise complete and accurate. | Compliance | @8h45k4r | 2026-09-18 | `LEGAL_GOVERNING_LAW` and `LEGAL_PRIVACY_EMAIL` set in the production environment, and both pages reviewed once by counsel in that jurisdiction. |
| 30 | Built front-end assets (`public/build`, 27 files, about 1 MB) are committed to the repository, because the production host does not build them: a Vite build needs Node plus roughly 200 MB of `node_modules`, which is most of the memory budget on the host this site shares with another application. The cost is that every front-end change carries a second, generated diff, and two branches touching the front end conflict in `public/build` even when their source changes do not overlap. A CI step guards the only failure that would reach production silently: it removes `public/build`, rebuilds, and fails if the result differs from what is committed, so a stale stylesheet cannot ship unnoticed. | Engineering | @8h45k4r | 2026-09-19 | Assets built in CI and published for the release script to fetch, so the repository carries source only. Needs a decision on where the artefact lives; it was raised and not taken, because it adds an automated public release stream to the repository. |
