# Vulnerability assessment, 11 September 2026

Scope: https://aipolicytracker.org (production), non-destructive, performed by the maintainer against the site they operate. Tooling: curl, openssl, npm audit, repository grep. No exploitation attempted beyond confirming behaviour.

| # | Check | Result | Severity | Action |
|---|-------|--------|----------|--------|
| 1 | Exposed files (`.env`, `.git/*`, `composer.json`, logs, `vendor/`, `resources/downloads/*`, debug tools) | 403/404 for all | — | None |
| 2 | Verbose errors / debug mode | No stack traces or debug pages | — | None |
| 3 | `X-Powered-By: PHP/8.3.x` disclosed | Present | Low | Fixed: header removed in middleware and hidden by nginx; `server_tokens off` |
| 4 | Content-Security-Policy | Absent | Medium | Fixed: nonce-based CSP (`default-src 'self'`, explicit analytics/font/CDN sources, `frame-ancestors 'self'`, `object-src 'none'`), inline scripts carry the Vite nonce |
| 5 | HSTS, nosniff, X-Frame-Options, Referrer-Policy, Permissions-Policy | Present | — | Added `Cross-Origin-Opener-Policy: same-origin` |
| 6 | Session cookie flags | `Secure; HttpOnly; SameSite=Lax`; XSRF token cookie readable by design | — | None |
| 7 | TLS | TLS 1.3, HTTP redirects to HTTPS | — | None |
| 8 | Unsafe methods (TRACE, OPTIONS, PUT on API) | 405 | — | None |
| 9 | Reflected input (`q` on policies and guides, HTML/JS payloads) | Fully HTML-escaped | — | None |
| 10 | Injection in filters and API parameters | Inputs whitelisted or parameterised; malformed values fall back or 404 | — | None |
| 11 | CSRF on public POST forms (subscribe, contribute, login) | 419 without token | — | None |
| 12 | Authorisation on download routes (`ready`, `file`) as guest / other user | Redirect to sign-in; other user 404; unsigned 403 (covered by tests) | — | None |
| 13 | Cron endpoint without / with wrong bearer token | 401 | — | None |
| 14 | Admin routes as guest | Redirect to sign-in | — | None |
| 15 | Open redirect via `redirect` parameter or legacy paths | Not honoured; legacy paths 301 to fixed internal pages | — | None |
| 16 | Throttling | Login throttled in-app (5/min per email+IP); contribute, subscribe, download POSTs throttled by route middleware | — | None |
| 17 | `npm audit --omit=dev` | 0 vulnerabilities after removing unused legacy packages (#48) | — | Dev-only advisories remain in build tooling |
| 18 | Secrets in repository | None found (placeholders only) | — | None |

Residual risk: every deploy on the free App Service tier causes a short outage and the container sometimes stalls afterwards (availability, not confidentiality); mitigated by `.github/workflows/uptime-self-heal.yml`; email verification is requested but not enforced before download (debt #19).
